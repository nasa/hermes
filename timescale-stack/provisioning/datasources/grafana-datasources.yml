apiVersion: 1

datasources:
  - name: TimescaleDB
    type: postgres
    access: proxy
    url: timescaledb:5432
    user: postgres
    database: hermes
    secureJsonData:
      password: "password"
    jsonData:
      sslmode: "disable"
      timescaledb: true
    isDefault: true
    editable: true
